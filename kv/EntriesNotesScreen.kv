<EntriesNotesScreen>:
    name: 'entries_notes_screen'
    on_pre_enter:
        root.init_entries_notes_screen()
    # on_pre_leave:
    BoxLayout:
        orientation: 'vertical'
        ScrollView:
            size_hint: 1, 0.9
            id: entries_sw_id
            TextInput:
                id: note_id
                text: root.note_text
                halign: "center"
                is_focusable: True
                hint_text: lang.get('entry_note_textinput_hint')
                font_size: config.get('font_size')
                write_tab: False
                multiline: True
        BoxLayout:
            orientation: 'horizontal'
            size_hint: 1, 0.1
            Button:
                text: lang.get('back_btn')
                font_size: config.get('font_size')
                height: "70dp"
                size_hint:(1, None)
                on_release:
                    root.back()

            Button:
                text: lang.get('save_btn')
                font_size: config.get('font_size')
                height: "70dp"
                size_hint:(1, None)
                on_release:
                    root.save_note()